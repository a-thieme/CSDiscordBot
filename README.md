# CSDiscordBot
```
~info [optional="class code"], retrieves the information on a course, or if no arg, if in a "COMP" channel, it will retrieve the info for that.
~info {"Professor name"}, same as above except required argument
~classes
~professors
```